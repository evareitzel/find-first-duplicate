  //// JS
  
  // const found = arr => arr.filter((value, index) => {
    // console.log(x)
    // console.log(x === arr[i])
    // x === arr[i] ? x : null
    // i++

  //   arr.indexOf(value) !== index
  // })

  // const duplicates = found(arr)
  // console.log(duplicates)

  // duplicates.length === 0 ? -1 : duplicates[0]
  // console.log(found)
  // return found


  // let index = 0 // , newArr = []
  // let newArr = []
  // const length = arr.length

  // // const findDuplicates = (arr) => {
  //   for (let i=0; i < length-1; i++) {
  //     // console.log(i)
  //     for (let j = i+1; j < length; j++) {
  //       // console.log(j)
  //       if (arr[i] === arr[j]) {
  //         // console.log(index)
  //         newArr[index] = arr[i]
  //         index++
  //       }
  //     }
  // // }
  // }

  // console.log(newArr)
  // newArr.length === 0 ? -1 : newArr[0]
  // return duplicates[0]

  // ## RUBY
  // # arr.each do |el|
  // #   duplicates << el if arr.count(el) > 1
  // # end
  // # duplicates.uniq[0]
